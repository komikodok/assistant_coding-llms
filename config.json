{
    "model": {
        "repo_id": "custom-llama3",
        "api_key": "<model_api_key> // use this if the model from api key"
    },
    "mongodb": {
        "db_name": "langchain",
        "collection_documents": "documents",
        "collection_chat_history": "chat_history",
        "index_name": "vector_index"
    }
}
